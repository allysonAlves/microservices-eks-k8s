# Chave mestra do auth-service para criar API keys
# Deve ser igual ao auth_master_key no terraform/terraform.tfvars
MASTER_KEY=change-me-use-a-strong-secret

# Chave gerada automaticamente pelo setup-service-key.sh
# Deixe em branco na primeira vez — o script preencherá
SERVICE_API_KEY=
